import { TemplatePlatform } from "../template.platform.js";

export class IranTalentPlatform extends TemplatePlatform {
  readonly id = "irantalent" as const;
  readonly displayName = "IranTalent";
}
